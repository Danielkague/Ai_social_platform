{
  "build": {
    "steps": [
      {
        "name": "build",
        "command": "python3 -m venv /app/venv && /app/venv/bin/pip install --upgrade pip && /app/venv/bin/pip install --only-binary=all -r requirements.txt"
      }
    ]
  },
  "deploy": {
    "startCommand": "/app/venv/bin/python Hope.py"
  }
} 