{
  "build": {
    "steps": [
      {
        "name": "build",
        "command": "chmod +x build.sh && ./build.sh"
      }
    ]
  },
  "deploy": {
    "startCommand": "/app/venv/bin/python ml-integration-example.py"
  }
} 